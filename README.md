# wren.zig

<a href="https://wren.io/">Wrenlang</a> rewritten in Zig.

> Work In Progress

## Instruction

Run the command with below for running the example script

```bash
zig build run -- examples/hello.wren
```
