pub const Value = @import("../vm/value.zig").Value;

pub const TokenType = enum {
    left_param,
    right_param,
    left_bracket,
    right_bracket,
    left_brace,
    right_brace,
    colon,
    dot,
    dotdot,
    dotdotdot,
    comma,
    star,
    slash,
    percent,
    hash,
    plus,
    minus,
    ltlt,
    gtgt,
    pipe,
    pipepipe,
    caret,
    amp,
    ampapm,
    bang,
    tilde,
    question,
    eq,
    lt,
    gt,
    lteq,
    gteq,
    eqeq,
    bangeq,

    @"break",
    @"continue",
    class,
    construct,
    @"else",
    false,
    @"for",
    foreign,
    @"if",
    import,
    as,
    in,
    is,
    null,
    @"return",
    static,
    super,
    this,
    true,
    @"var",
    @"while",
    field,
    static_field,
    name,
    number,
    string,
    interpolation,
    line,
    @"error",
    eof,
};

const Token = struct {
    type: TokenType,
    start: []const u8,
    length: i32,
    line: i32,
    value: Value,
};
